<Page x:Class="BianChat.Views.ChatPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:BianChat.Views"
      xmlns:ui="http://schemas.modernwpf.com/2019"
      xmlns:controls="clr-namespace:BianChat.Controls"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="ChatPage">
    <Grid>
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="*"/>
                <RowDefinition Height="50"/>
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="200"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="60"/>
            </Grid.ColumnDefinitions>
            <ui:ListView x:Name="UserList" Grid.Column="0" Grid.Row="0"
                         Grid.RowSpan="2" Background="LightGray"/>
            <TextBox x:Name="Message" Grid.Column="1" Grid.Row="1"
                     Margin="10 0 10 10"/>
            <Button x:Name="SendButton" Grid.Column="2" Grid.Row="1"
                    Margin="0 0 0 10" Click="SendButton_Click" IsDefault="True">发送</Button>
        </Grid>
    </Grid>
</Page>
